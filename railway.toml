[deploy]
startCommand = "python manage.py migrate && gunicorn corobimy.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/admin/login/"
healthcheckTimeout = 30

[build]
builder = "RAILPACK"
