[deploy]
startCommand = "python manage.py collectstatic --no-input && python manage.py migrate && gunicorn corobimy.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --access-logfile -"
healthcheckPath = "/health/"
healthcheckTimeout = 30

[build]
builder = "RAILPACK"
